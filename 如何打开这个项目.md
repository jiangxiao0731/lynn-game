# 如何打开这个项目

这是 **Godot 源码工程**，不是可以直接双击运行的游戏。想直接玩的话，要的是导出好的
`ShallowSeaDream.app`，不是这个文件夹。

## 需要装的东西

| | 版本 | 说明 |
|---|---|---|
| Godot | **4.7 的 .NET / Mono 版** | 必须是 .NET 版，标准版打不开——本项目用 C# 写 |
| .NET SDK | 8.0 或更高 | Godot 只负责引擎，C# 编译要靠它 |

下载：
- Godot（在页面上认准 **.NET** 那一栏）https://godotengine.org/download/
- .NET SDK https://dotnet.microsoft.com/download

版本要对得上：项目声明的是 `config/features = ("4.7", "C#", "Forward Plus")`。
用 4.6 或更早的 Godot 打开会报错。

## 打开步骤

1. 解压到任意位置（路径里别带中文或空格更保险）
2. 打开 Godot 4.7 .NET 版 → **Import** → 选中本文件夹里的 `project.godot`
3. 首次导入要等一会儿：43 张素材要重新导入，其中 9 张背景是 6000px 的大图
4. 导入完成后按 **F5** 运行

第一次导入过程中控制台可能闪过几条资源顺序的报错（字体还没导完就被主题引用了），
这是冷启动的正常现象。**再按一次 F5 或重开一次编辑器**就干净了。

## 如果 C# 报错 / 脚本全红

先在项目根目录跑一次：

```
dotnet build
```

正常应该输出 `Build succeeded`。

本项目所依赖的 `Godot.NET.Sdk 4.7.0` 没有发布到 nuget.org，只随 Godot 4.7 .NET 版
附带。为了让别人的机器也能编译，这几个包已经放在项目内的 `nupkgs/` 目录里，
`nuget.config` 用相对路径引用它们。**不要删 `nupkgs/` 文件夹。**

## 目录说明

| | |
|---|---|
| `scripts/` | 全部 C# 游戏逻辑 |
| `scenes/` | 三个关卡场景 + 标题 / 教程 / 结算面板 |
| `assets/` | 运行时用到的图、字体、主题 |
| `source_art/` | 项目自带的手绘源图 |
| `nupkgs/` | 编译所需的 Godot NuGet 包，别删 |
| `build/` | 已导出的 macOS 版，可以直接玩 |

启动场景是 `scenes/game_scene1.tscn`，直接进第一关，不走标题和教程。
